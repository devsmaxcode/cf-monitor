{
  "name": "cf-cache-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts --port 3033",
    "dev": "bun run src/server.ts --port 3033",
    "build": "bun build src/server.ts --target bun --outdir dist --root .",
    "monitor": "bun run scripts/cloudflare_cache_monitor_bun.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "portless": "^0.14.0"
  },
  "dependencies": {
    "croner": "^10.0.1"
  }
}
