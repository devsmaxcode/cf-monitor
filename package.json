{
  "name": "cf-cache-monitor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "portless run bun run src/server.ts",
    "monitor": "bun run scripts/cloudflare_cache_monitor_bun.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "portless": "^0.14.0"
  },
  "dependencies": {
    "croner": "^10.0.1"
  }
}
